{
  "name": "referral-race",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-apps-script.mjs",
    "build:static": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  },
  "engines": {
    "node": ">=20"
  }
}
